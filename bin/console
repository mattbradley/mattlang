#!/usr/bin/env ruby

require "bundler/setup"
require "byebug"

require "mattlang"
include Mattlang

require "pry"

Pry.hooks.add_hook(:before_eval, 'wrap with pry-rescue') do |code, _pry_|
  code.prepend 'Pry::rescue { '
  code << ' }'
end

Pry.start
