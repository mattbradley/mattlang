#include "types.h"

mt_Type mt_Int = {.kind = MT_GROUND_TYPE};
mt_Type mt_Float = {.kind = MT_GROUND_TYPE};

mt_Type mt_types_make_union(mt_Type* types) {
    
}

mt_Type* mt_types_remove_duplicates(mt_Type* types) {

}
